# auto-resource-allocation

全自动资源分配skill - dynamically schedule multiple parallel projects based on priority, maximize idle resource utilization, speed up project completion without quality loss.

## Key Features

- Automatic priority detection
- Dynamic load balancing
- Progress tracking
- Deadline monitoring
- Parallel execution without conflicts

## Token Saving

All scheduling logic runs locally via CLI, only summary sent to chat - minimal token usage.
